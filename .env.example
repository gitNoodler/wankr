# =============================================================================
# xAI / Grok — Infisical only (no .env fallback for API keys)
# =============================================================================
#
# xAI key MUST be stored in Infisical as XAI_API_KEY or grokWankr.
# In Infisical dashboard also add: VITE_API_KEY = your-key (frontend gets it via GET /api/config).
#
# Infisical Machine Identity (REQUIRED):
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
INFISICAL_PROJECT_ID=6c43f69b-0066-4ef6-b2b7-f82b1e6f41d5
INFISICAL_ENVIRONMENT=dev
#
# Run: wankr.bat (or start_backend.bat for API only)
#
# Optional — Cloudflare Tunnel (so wankrbot.com reaches this backend):
# CLOUDFLARE_TUNNEL_TOKEN=eyJ...  (from Zero Trust ^> Tunnels ^> your tunnel ^> Run command; use RUN token, not "service install")
# TUNNEL_PROTOCOL=quic   (optional; default is http2 to avoid "control stream" errors on Windows)
#
# Optional — Proxy UI to Vite in dev (so opening http://localhost:5000 shows live app from 5173):
# PROXY_UI_TO_VITE=1
# DEV_UI_PORT=5173   (default; Vite dev server port)
# =============================================================================
