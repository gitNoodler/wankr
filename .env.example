# =============================================================================
# No raw xAI keys in .env. Use Infisical (Option 1 or Option 2).
# =============================================================================
#
# Option 1 — Fastest (no keys in .env):
#   Infisical → Wankr project → Secrets (dev):
#     XAI_API_KEY   = your xai-XXXXXXXXXXXXXXXX key
#     XAI_BASE_URL  = https://api.x.ai/v1
#   Run:  run_with_infisical.bat
#   Test (PowerShell): Invoke-RestMethod -Uri http://127.0.0.1:5000/api/chat -Method POST -ContentType application/json -Body '{"message":"who are you"}'
#
# Option 2 — Machine Identity only in .env (set these, app fetches xAI from Infisical):
# =============================================================================

INFISICAL_CLIENT_ID=your-machine-identity-client-id
INFISICAL_CLIENT_SECRET=your-machine-identity-client-secret
INFISICAL_PROJECT_ID=your-project-id
INFISICAL_ENVIRONMENT=dev
