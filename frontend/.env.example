# Frontend Environment Variables
# Copy to .env for local dev. For production deploy, set VITE_API_BASE when building.

# Backend API URL. Leave unset in dev (Vite proxies /api to localhost:5000).
# For production: set to your deployed backend URL before `npm run build` or `npm run deploy`.
# VITE_API_BASE=https://your-backend.up.railway.app
