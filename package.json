{
  "name": "wankr",
  "private": true,
  "scripts": {
    "dev": "wankr.bat",
    "build": "cd frontend && npm run build",
    "sync-online": "cd frontend && npm run clear-cache && npm run build"
  }
}
