# Backend Environment Variables
# Set these in Railway dashboard

# xAI API Key — MUST be set in Infisical (no .env fallback)
# Store as XAI_API_KEY or grokWankr in your Infisical project

# Port (Railway sets this automatically)
PORT=5000

# Infisical (REQUIRED for xAI key)
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
INFISICAL_PROJECT_ID=
INFISICAL_ENVIRONMENT=dev
